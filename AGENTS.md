# RULES
- When done with a coding task or a coding test task always check your memory notes and update it in MEMORY.md
- When working on code tests put it into folder "tests".
- Helper scripts prefixed with `_` belong in either `tests/` (test utilities) or `debug/` (debugging/inspection scripts).
